"use strict";

const answerGeneratorPrompt = `Use only supplied university and department records. Do not invent universities, departments, rankings, admission odds, scores, employment claims, or characteristics. If a related major is used, state that it is related matching.`;

module.exports = { answerGeneratorPrompt };
